import fs from "node:fs";import path from "node:path";import url from "node:url";
const here=path.dirname(url.fileURLToPath(import.meta.url)),root=path.resolve(here,"..");let checks=0,failures=0;const expect=(ok,msg)=>{checks++;if(!ok){failures++;console.error(`FAIL: ${msg}`);}};const read=(p)=>fs.readFileSync(path.join(root,p),"utf8");
const moduleJson=JSON.parse(read("module.json"));expect(["0.4.0","0.5.0"].includes(moduleJson.version),"module is older than 0.4 motion baseline");expect(moduleJson.esmodules.includes("scripts/animations-v04.js"),"0.4 animation layer is not loaded");expect(moduleJson.styles.includes("styles/animations-v04.css"),"0.4 animation CSS is not loaded");
const animation=read("scripts/animations-v04.js");for(const feature of ["motionLevel","prefers-reduced-motion","renderApplication","currentNodeId","v04-decrypted","v04-pip-spent","v04-program-rez","v04-motion-subtle"])expect(animation.includes(feature),`animation feature missing: ${feature}`);expect(animation.includes("packet.animate("),"route packet does not use bounded Web Animations");expect(!animation.includes("setInterval("),"animation layer must not run a permanent JS interval");
const css=read("styles/animations-v04.css");for(const feature of ["v04-motion-off","v04-motion-cinematic","crnsl-v04-flow","crnsl-v04-decrypt","prefers-reduced-motion"])expect(css.includes(feature),`motion CSS missing: ${feature}`);
const doc=read("ANIMATION-DESIGN.md");for(const phrase of ["Foundry VTT v12","Animation follows truth","No fake combat","prefers-reduced-motion"])expect(doc.includes(phrase),`animation design note missing: ${phrase}`);
console.log(`Lab 0.4 motion baseline: ${checks}, failures: ${failures}`);process.exit(failures?1:0);
